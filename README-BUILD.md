This is a compile-ready project layout created from the source files you uploaded.

Build:
  dotnet restore RookieMacOS.sln
  dotnet build RookieMacOS.sln

Run:
  dotnet run --project RookieMacOS.UI/RookieMacOS.UI.csproj
